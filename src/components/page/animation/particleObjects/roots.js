/**
 * ============================================================================
 * RADICI
 * ============================================================================
 * 
 * Responsabilità: Creare solo le radici degli alberi
 */

/**
 * Crea le particelle per le radici di un albero
 * @param {Object} config - Configurazione (treeX, baseY, houseSize, etc.)
 * @param {number} maxParticles - Numero massimo di particelle per radici
 * @param {Array} positions - Array dove aggiungere le posizioni
 * @param {Array} colors - Array dove aggiungere i colori
 * @param {Float32Array} scaleArray - Array per le scale
 * @param {number} startIndex - Indice iniziale nell'array scale
 * @returns {number} - Numero di particelle create
 */
export function createRootsParticles(config, maxParticles, positions, colors, scaleArray, startIndex) {
  // Le radici non sono ancora implementate nel codice attuale
  // Questo file è preparato per future implementazioni
  // Per ora ritorna 0 (nessuna particella creata)
  return 0
}

